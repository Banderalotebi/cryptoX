{
  "name": "server-wallet",
  "version": "1.0.0",
  "description": "A complete Ethereum server wallet project with smart contracts and backend API",
  "main": "backend/index.js",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network goerli",
    "start": "node backend/index.js"
  },
  "keywords": ["ethereum", "wallet", "smart-contract", "hardhat", "express"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "hardhat": "^2.19.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.8.0",
    "express": "^4.18.2"
  }
}
